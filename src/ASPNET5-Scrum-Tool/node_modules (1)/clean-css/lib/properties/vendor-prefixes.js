var VENDOR_PREFIX_PATTERN = /$\-moz\-|\-ms\-|\-o\-|\-webkit\-/;

function prefixesIn(tokens) ***REMOVED***
  var prefixes = [];

  for (var i = 0, l = tokens.length; i < l; i++) ***REMOVED***
    var token = tokens[i];

    for (var j = 0, m = token.value.length; j < m; j++) ***REMOVED***
      var match = VENDOR_PREFIX_PATTERN.exec(token.value[j][0]);

      if (match && prefixes.indexOf(match[0]) == -1)
        prefixes.push(match[0]);
***REMOVED***
  ***REMOVED***

  return prefixes;
***REMOVED***

function same(left, right) ***REMOVED***
  return prefixesIn(left).sort().join(',') == prefixesIn(right).sort().join(',');
***REMOVED***

module.exports = ***REMOVED***
  same: same
***REMOVED***;
