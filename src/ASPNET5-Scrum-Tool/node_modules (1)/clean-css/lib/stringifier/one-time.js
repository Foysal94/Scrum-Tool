var helpers = require('./helpers');

function store(token, context) ***REMOVED***
  context.output.push(typeof token == 'string' ? token : token[0]);
***REMOVED***

function context() ***REMOVED***
  return ***REMOVED***
    output: [],
    store: store
  ***REMOVED***;
***REMOVED***

function all(tokens) ***REMOVED***
  var fakeContext = context();
  helpers.all(tokens, fakeContext);
  return fakeContext.output.join('');
***REMOVED***

function body(tokens) ***REMOVED***
  var fakeContext = context();
  helpers.body(tokens, fakeContext);
  return fakeContext.output.join('');
***REMOVED***

function property(tokens, position) ***REMOVED***
  var fakeContext = context();
  helpers.property(tokens, position, true, fakeContext);
  return fakeContext.output.join('');
***REMOVED***

function selectors(tokens) ***REMOVED***
  var fakeContext = context();
  helpers.selectors(tokens, fakeContext);
  return fakeContext.output.join('');
***REMOVED***

function value(tokens, position) ***REMOVED***
  var fakeContext = context();
  helpers.value(tokens, position, true, fakeContext);
  return fakeContext.output.join('');
***REMOVED***

module.exports = ***REMOVED***
  all: all,
  body: body,
  property: property,
  selectors: selectors,
  value: value
***REMOVED***;
