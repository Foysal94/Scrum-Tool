function cloneArray(array) ***REMOVED***
  var cloned = array.slice(0);

  for (var i = 0, l = cloned.length; i < l; i++) ***REMOVED***
    if (Array.isArray(cloned[i]))
      cloned[i] = cloneArray(cloned[i]);
  ***REMOVED***

  return cloned;
***REMOVED***

module.exports = cloneArray;
