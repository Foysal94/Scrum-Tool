'use strict'

var runner = require('./runner')
var schemas = require('./schemas')

module.exports = function (data, cb) ***REMOVED***
  return runner(schemas.har, data, cb)
***REMOVED***

Object.keys(schemas).map(function (name) ***REMOVED***
  module.exports[name] = function (data, cb) ***REMOVED***
    return runner(schemas[name], data, cb)
  ***REMOVED***
***REMOVED***)
