'use strict'

var Promise = require('pinkie-promise')
var runner = require('./runner')
var schemas = require('./schemas')

var promisify = function (schema) ***REMOVED***
  return function (data) ***REMOVED***
    return new Promise(function (resolve, reject) ***REMOVED***
      runner(schema, data, function (err, valid) ***REMOVED***
        return err === null ? resolve(data) : reject(err)
  ***REMOVED***)
***REMOVED***)
  ***REMOVED***
***REMOVED***

module.exports = promisify(schemas.har)

// utility methods for all parts of the schema
Object.keys(schemas).map(function (name) ***REMOVED***
  module.exports[name] = promisify(schemas[name])
***REMOVED***)
