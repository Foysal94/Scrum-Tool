var http = require('http')
  , https = require('https')
  , server = http.createServer(handler)
  , port = +process.argv[2]
  , prefix = process.argv[3]
  , upstream = process.argv[4]
  , calls = 0

server.listen(port)

function handler (req, res) ***REMOVED***
  if (req.url.indexOf(prefix) != 0)
    throw new Error('request url [' + req.url + '] does not start with [' + prefix + ']')

  var upstreamUrl = upstream + req.url.substring(prefix.length)
  console.log(req.url + ' -> ' + upstreamUrl)
  https.get(upstreamUrl, function (ures) ***REMOVED***
    ures.on('end', function () ***REMOVED***
      if (++calls == 2)
        server.close()
***REMOVED***)
    ures.pipe(res)
  ***REMOVED***)
***REMOVED***
