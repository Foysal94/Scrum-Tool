#include <cctype>
#include <cstddef>
#include <iostream>
#include <iomanip>
#include "util.hpp"
#include "position.hpp"
#include "prelexer.hpp"
#include "constants.hpp"


namespace Sass ***REMOVED***
  // using namespace Lexer;
  using namespace Constants;

  namespace Prelexer ***REMOVED***


    // Match a line comment (/.*?(?=\n|\r\n?|\Z)/.
    const char* line_comment(const char* src)
***REMOVED***
      return sequence<
               exactly <
                 slash_slash
               >,
               non_greedy<
                 any_char,
                 end_of_line
               >
             >(src);
***REMOVED***

    // Match a block comment.
    const char* block_comment(const char* src)
***REMOVED***
      return sequence<
               delimited_by<
                 slash_star,
                 star_slash,
                 false
               >
             >(src);
***REMOVED***
    /* not use anymore - remove?
    const char* block_comment_prefix(const char* src) ***REMOVED***
      return exactly<slash_star>(src);
***REMOVED***
    // Match either comment.
    const char* comment(const char* src) ***REMOVED***
      return line_comment(src);
***REMOVED***
    */

    // Match zero plus white-space or line_comments
    const char* optional_css_whitespace(const char* src) ***REMOVED***
      return zero_plus< alternatives<spaces, line_comment> >(src);
***REMOVED***
    const char* css_whitespace(const char* src) ***REMOVED***
      return one_plus< alternatives<spaces, line_comment> >(src);
***REMOVED***
    // Match optional_css_whitepace plus block_comments
    const char* optional_css_comments(const char* src) ***REMOVED***
      return zero_plus< alternatives<spaces, line_comment, block_comment> >(src);
***REMOVED***
    const char* css_comments(const char* src) ***REMOVED***
      return one_plus< alternatives<spaces, line_comment, block_comment> >(src);
***REMOVED***

    // Match one backslash escaped char /\\./
    const char* escape_seq(const char* src)
***REMOVED***
      return sequence<
        exactly<'\\'>,
        alternatives <
          minmax_range<
            1, 3, xdigit
          >,
          any_char
        >,
        optional <
          exactly <' '>
        >
      >(src);
***REMOVED***

    // Match identifier start
    const char* identifier_alpha(const char* src)
***REMOVED***
      return alternatives<
               unicode_seq,
               alpha,
               unicode,
               exactly<'-'>,
               exactly<'_'>,
               NONASCII,
               ESCAPE,
               escape_seq
             >(src);
***REMOVED***

    // Match identifier after start
    const char* identifier_alnum(const char* src)
***REMOVED***
      return alternatives<
               unicode_seq,
               alnum,
               unicode,
               exactly<'-'>,
               exactly<'_'>,
               NONASCII,
               ESCAPE,
               escape_seq
             >(src);
***REMOVED***

    // Match CSS identifiers.
    const char* strict_identifier(const char* src)
***REMOVED***
      return sequence<
               one_plus < strict_identifier_alpha >,
               zero_plus < strict_identifier_alnum >
               // word_boundary not needed
             >(src);
***REMOVED***

    // Match CSS identifiers.
    const char* identifier(const char* src)
***REMOVED***
      return sequence<
               zero_plus< exactly<'-'> >,
               one_plus < identifier_alpha >,
               zero_plus < identifier_alnum >
               // word_boundary not needed
             >(src);
***REMOVED***

    const char* strict_identifier_alpha(const char* src)
***REMOVED***
      return alternatives <
               alpha,
               unicode,
               escape_seq,
               exactly<'_'>
             >(src);
***REMOVED***

    const char* strict_identifier_alnum(const char* src)
***REMOVED***
      return alternatives <
               alnum,
               unicode,
               escape_seq,
               exactly<'_'>
             >(src);
***REMOVED***

    // Match CSS unit identifier.
    const char* unit_identifier(const char* src)
***REMOVED***
      return sequence <
               optional < exactly <'-'> >,
               strict_identifier_alpha,
               zero_plus < alternatives<
                 strict_identifier_alnum,
                 sequence <
                   one_plus < exactly<'-'> >,
                   strict_identifier_alpha
                 >
               > >
             >(src);
***REMOVED***

    const char* identifier_alnums(const char* src)
***REMOVED***
      return one_plus< identifier_alnum >(src);
***REMOVED***

    // Match number prefix ([\+\-]+)
    const char* number_prefix(const char* src) ***REMOVED***
      return alternatives <
        exactly < '+' >,
        sequence <
          exactly < '-' >,
          optional_css_whitespace,
          exactly< '-' >
        >
      >(src);
***REMOVED***

    // Match interpolant schemas
    const char* identifier_schema(const char* src) ***REMOVED***

      return sequence <
               one_plus <
                 sequence <
                   zero_plus <
                     alternatives <
                       identifier,
                       exactly <'-'>
                     >
                   >,
                   interpolant,
                   zero_plus <
                     alternatives <
                       digits,
                       identifier,
                       quoted_string,
                       exactly<'+'>,
                       exactly<'-'>
                     >
                   >
                 >
               >,
               negate <
                 exactly<'%'>
               >
             > (src);
***REMOVED***

    // interpolants can be recursive/nested
    const char* interpolant(const char* src) ***REMOVED***
      return recursive_scopes< exactly<hash_lbrace>, exactly<rbrace> >(src);
***REMOVED***

    // $re_squote = /'(?:$re_itplnt|\\.|[^'])*'/
    const char* single_quoted_string(const char* src) ***REMOVED***
      // match a single quoted string, while skipping interpolants
      return sequence <
        exactly <'\''>,
        zero_plus <
          alternatives <
            // skip escapes
            sequence <
              exactly < '\\' >,
              re_linebreak
            >,
            escape_seq,
            unicode_seq,
            // skip interpolants
            interpolant,
            // skip non delimiters
            any_char_but < '\'' >
          >
        >,
        exactly <'\''>
      >(src);
***REMOVED***

    // $re_dquote = /"(?:$re_itp|\\.|[^"])*"/
    const char* double_quoted_string(const char* src) ***REMOVED***
      // match a single quoted string, while skipping interpolants
      return sequence <
        exactly <'"'>,
        zero_plus <
          alternatives <
            // skip escapes
            sequence <
              exactly < '\\' >,
              re_linebreak
            >,
            escape_seq,
            unicode_seq,
            // skip interpolants
            interpolant,
            // skip non delimiters
            any_char_but < '"' >
          >
        >,
        exactly <'"'>
      >(src);
***REMOVED***

    // $re_quoted = /(?:$re_squote|$re_dquote)/
    const char* quoted_string(const char* src) ***REMOVED***
      // match a quoted string, while skipping interpolants
      return alternatives<
        single_quoted_string,
        double_quoted_string
      >(src);
***REMOVED***

    const char* value_schema(const char* src) ***REMOVED***
      // follows this pattern: ([xyz]*i[xyz]*)+
      return one_plus< sequence< zero_plus< alternatives< identifier, percentage, dimension, hex, number, quoted_string > >,
                                 interpolant,
                                 zero_plus< alternatives< identifier, percentage, dimension, hex, number, quoted_string, exactly<'%'> > > > >(src);
***REMOVED***

    /* not used anymore - remove?
    const char* filename(const char* src) ***REMOVED***
      return one_plus< alternatives< identifier, number, exactly<'.'> > >(src);
***REMOVED***
    */

    // Match CSS '@' keywords.
    const char* at_keyword(const char* src) ***REMOVED***
      return sequence<exactly<'@'>, identifier>(src);
***REMOVED***

    const char* re_reference_combinator(const char* src) ***REMOVED***
      return sequence <
        optional <
          sequence <
            zero_plus <
              exactly <'-'>
            >,
            identifier,
            exactly <'|'>
          >
        >,
        zero_plus <
          exactly <'-'>
        >,
        identifier
      >(src);
***REMOVED***

    const char* static_reference_combinator(const char* src) ***REMOVED***
      return sequence <
        exactly <'/'>,
        re_reference_combinator,
        exactly <'/'>
      >(src);
***REMOVED***

    const char* schema_reference_combinator(const char* src) ***REMOVED***
      return sequence <
        exactly <'/'>,
        optional <
          sequence <
            css_ip_identifier,
            exactly <'|'>
          >
        >,
        css_ip_identifier,
        exactly <'/'>
      > (src);
***REMOVED***

    const char* kwd_import(const char* src) ***REMOVED***
      return word<import_kwd>(src);
***REMOVED***

    const char* kwd_at_root(const char* src) ***REMOVED***
      return word<at_root_kwd>(src);
***REMOVED***

    const char* kwd_with_directive(const char* src) ***REMOVED***
      return word<with_kwd>(src);
***REMOVED***

    const char* kwd_without_directive(const char* src) ***REMOVED***
      return word<without_kwd>(src);
***REMOVED***

    const char* kwd_media(const char* src) ***REMOVED***
      return word<media_kwd>(src);
***REMOVED***

    const char* kwd_supports_directive(const char* src) ***REMOVED***
      return word<supports_kwd>(src);
***REMOVED***

    const char* kwd_mixin(const char* src) ***REMOVED***
      return word<mixin_kwd>(src);
***REMOVED***

    const char* kwd_function(const char* src) ***REMOVED***
      return word<function_kwd>(src);
***REMOVED***

    const char* kwd_return_directive(const char* src) ***REMOVED***
      return word<return_kwd>(src);
***REMOVED***

    const char* kwd_include_directive(const char* src) ***REMOVED***
      return word<include_kwd>(src);
***REMOVED***

    const char* kwd_content_directive(const char* src) ***REMOVED***
      return word<content_kwd>(src);
***REMOVED***

    const char* kwd_charset_directive(const char* src) ***REMOVED***
      return word<charset_kwd>(src);
***REMOVED***

    const char* kwd_extend(const char* src) ***REMOVED***
      return word<extend_kwd>(src);
***REMOVED***


    const char* kwd_if_directive(const char* src) ***REMOVED***
      return word<if_kwd>(src);
***REMOVED***

    const char* kwd_else_directive(const char* src) ***REMOVED***
      return word<else_kwd>(src);
***REMOVED***
    const char* elseif_directive(const char* src) ***REMOVED***
      return sequence< exactly< else_kwd >,
                                optional_css_comments,
                                word< if_after_else_kwd > >(src);
***REMOVED***

    const char* kwd_for_directive(const char* src) ***REMOVED***
      return word<for_kwd>(src);
***REMOVED***

    const char* kwd_from(const char* src) ***REMOVED***
      return word<from_kwd>(src);
***REMOVED***

    const char* kwd_to(const char* src) ***REMOVED***
      return word<to_kwd>(src);
***REMOVED***

    const char* kwd_through(const char* src) ***REMOVED***
      return word<through_kwd>(src);
***REMOVED***

    const char* kwd_each_directive(const char* src) ***REMOVED***
      return word<each_kwd>(src);
***REMOVED***

    const char* kwd_in(const char* src) ***REMOVED***
      return word<in_kwd>(src);
***REMOVED***

    const char* kwd_while_directive(const char* src) ***REMOVED***
      return word<while_kwd>(src);
***REMOVED***

    const char* name(const char* src) ***REMOVED***
      return one_plus< alternatives< alnum,
                                     exactly<'-'>,
                                     exactly<'_'>,
                                     exactly<'\\'> > >(src);
***REMOVED***

    const char* kwd_warn(const char* src) ***REMOVED***
      return word<warn_kwd>(src);
***REMOVED***

    const char* kwd_err(const char* src) ***REMOVED***
      return word<error_kwd>(src);
***REMOVED***

    const char* kwd_dbg(const char* src) ***REMOVED***
      return word<debug_kwd>(src);
***REMOVED***

    /* not used anymore - remove?
    const char* directive(const char* src) ***REMOVED***
      return sequence< exactly<'@'>, identifier >(src);
***REMOVED*** */

    const char* kwd_null(const char* src) ***REMOVED***
      return word<null_kwd>(src);
***REMOVED***

    const char* css_identifier(const char* src) ***REMOVED***
      return sequence <
               zero_plus <
                 exactly <'-'>
               >,
               identifier
             >(src);
***REMOVED***

    const char* css_ip_identifier(const char* src) ***REMOVED***
      return sequence <
               zero_plus <
                 exactly <'-'>
               >,
               alternatives <
                 identifier,
                 interpolant
               >
             >(src);
***REMOVED***

    // Match CSS type selectors
    const char* namespace_prefix(const char* src) ***REMOVED***
      return sequence <
               optional <
                 alternatives <
                   exactly <'*'>,
                   css_identifier
                 >
               >,
               exactly <'|'>,
               negate <
                 exactly <'='>
               >
             >(src);
***REMOVED***

    // Match CSS type selectors
    const char* namespace_schema(const char* src) ***REMOVED***
      return sequence <
               optional <
                 alternatives <
                   exactly <'*'>,
                   css_ip_identifier
                 >
               >,
               exactly<'|'>,
               negate <
                 exactly <'='>
               >
             >(src);
***REMOVED***

    const char* hyphens_and_identifier(const char* src) ***REMOVED***
      return sequence< zero_plus< exactly< '-' > >, identifier >(src);
***REMOVED***
    const char* hyphens_and_name(const char* src) ***REMOVED***
      return sequence< zero_plus< exactly< '-' > >, name >(src);
***REMOVED***
    const char* universal(const char* src) ***REMOVED***
      return sequence< optional<namespace_schema>, exactly<'*'> >(src);
***REMOVED***
    // Match CSS id names.
    const char* id_name(const char* src) ***REMOVED***
      return sequence<exactly<'#'>, name>(src);
***REMOVED***
    // Match CSS class names.
    const char* class_name(const char* src) ***REMOVED***
      return sequence<exactly<'.'>, identifier>(src);
***REMOVED***
    // Attribute name in an attribute selector.
    const char* attribute_name(const char* src) ***REMOVED***
      return alternatives< sequence< optional<namespace_schema>, identifier>,
                           identifier >(src);
***REMOVED***
    // match placeholder selectors
    const char* placeholder(const char* src) ***REMOVED***
      return sequence<exactly<'%'>, identifier>(src);
***REMOVED***
    // Match CSS numeric constants.

    const char* sign(const char* src) ***REMOVED***
      return class_char<sign_chars>(src);
***REMOVED***
    const char* unsigned_number(const char* src) ***REMOVED***
      return alternatives<sequence< zero_plus<digits>,
                                    exactly<'.'>,
                                    one_plus<digits> >,
                          digits>(src);
***REMOVED***
    const char* number(const char* src) ***REMOVED***
      return sequence< optional<sign>, unsigned_number>(src);
***REMOVED***
    const char* coefficient(const char* src) ***REMOVED***
      return alternatives< sequence< optional<sign>, digits >,
                           sign >(src);
***REMOVED***
    const char* binomial(const char* src) ***REMOVED***
      return sequence <
               optional < sign >,
               optional < digits >,
               exactly <'n'>,
               zero_plus < sequence <
                 optional_css_whitespace, sign,
                 optional_css_whitespace, digits
               > >
             >(src);
***REMOVED***
    const char* percentage(const char* src) ***REMOVED***
      return sequence< number, exactly<'%'> >(src);
***REMOVED***
    const char* ampersand(const char* src) ***REMOVED***
      return exactly<'&'>(src);
***REMOVED***

    /* not used anymore - remove?
    const char* em(const char* src) ***REMOVED***
      return sequence< number, exactly<em_kwd> >(src);
***REMOVED*** */
    const char* dimension(const char* src) ***REMOVED***
      return sequence<number, unit_identifier >(src);
***REMOVED***
    const char* hex(const char* src) ***REMOVED***
      const char* p = sequence< exactly<'#'>, one_plus<xdigit> >(src);
      ptrdiff_t len = p - src;
      return (len != 4 && len != 7) ? 0 : p;
***REMOVED***
    const char* hexa(const char* src) ***REMOVED***
      const char* p = sequence< exactly<'#'>, one_plus<xdigit> >(src);
      ptrdiff_t len = p - src;
      return (len != 4 && len != 7 && len != 9) ? 0 : p;
***REMOVED***
    const char* hex0(const char* src) ***REMOVED***
      const char* p = sequence< exactly<'0'>, exactly<'x'>, one_plus<xdigit> >(src);
      ptrdiff_t len = p - src;
      return (len != 5 && len != 8) ? 0 : p;
***REMOVED***

    /* no longer used - remove?
    const char* rgb_prefix(const char* src) ***REMOVED***
      return word<rgb_kwd>(src);
***REMOVED****/
    // Match CSS uri specifiers.

    const char* uri_prefix(const char* src) ***REMOVED***
      return exactly<url_kwd>(src);
***REMOVED***
    const char* uri_value(const char* src)
***REMOVED***
      return
      sequence <
        negate <
          exactly < '$' >
        >,
        zero_plus <
          alternatives <
            alnum,
            interpolant,
            exactly <'/'>,
            class_char < uri_chars >
          >
        >
      >(src);
***REMOVED***

    // TODO: rename the following two functions
    /* no longer used - remove?
    const char* uri(const char* src) ***REMOVED***
      return sequence< exactly<url_kwd>,
                       optional<spaces>,
                       quoted_string,
                       optional<spaces>,
                       exactly<')'> >(src);
***REMOVED****/
    /* no longer used - remove?
    const char* url_value(const char* src) ***REMOVED***
      return sequence< optional< sequence< identifier, exactly<':'> > >, // optional protocol
                       one_plus< sequence< zero_plus< exactly<'/'> >, filename > >, // one or more folders and/or trailing filename
                       optional< exactly<'/'> > >(src);
***REMOVED****/
    /* no longer used - remove?
    const char* url_schema(const char* src) ***REMOVED***
      return sequence< optional< sequence< identifier, exactly<':'> > >, // optional protocol
                       filename_schema >(src); // optional trailing slash
***REMOVED****/
    // Match CSS "!important" keyword.
    const char* kwd_important(const char* src) ***REMOVED***
      return sequence< exactly<'!'>,
                       optional_css_whitespace,
                       word<important_kwd> >(src);
***REMOVED***
    // Match CSS "!optional" keyword.
    const char* kwd_optional(const char* src) ***REMOVED***
      return sequence< exactly<'!'>,
      optional_css_whitespace,
      word<optional_kwd> >(src);
***REMOVED***
    // Match Sass "!default" keyword.
    const char* default_flag(const char* src) ***REMOVED***
      return sequence< exactly<'!'>,
                       optional_css_whitespace,
                       word<default_kwd> >(src);
***REMOVED***
    // Match Sass "!global" keyword.
    const char* global_flag(const char* src) ***REMOVED***
      return sequence< exactly<'!'>,
                       optional_css_whitespace,
                       word<global_kwd> >(src);
***REMOVED***
    // Match CSS pseudo-class/element prefixes.
    const char* pseudo_prefix(const char* src) ***REMOVED***
      return sequence< exactly<':'>, optional< exactly<':'> > >(src);
***REMOVED***
    // Match CSS function call openers.
    const char* functional_schema(const char* src) ***REMOVED***
      return sequence <
               one_plus <
                 sequence <
                   zero_plus <
                     alternatives <
                       identifier,
                       exactly <'-'>
                     >
                   >,
                   one_plus <
                     sequence <
                       interpolant,
                       alternatives <
                         digits,
                         identifier,
                         exactly<'+'>,
                         exactly<'-'>
                       >
                     >
                   >
                 >
               >,
               negate <
                 exactly <'%'>
               >,
               lookahead <
                 exactly <'('>
               >
             > (src);
***REMOVED***

    const char* re_nothing(const char* src) ***REMOVED***
      return src;
***REMOVED***

    const char* re_functional(const char* src) ***REMOVED***
      return sequence< identifier, optional < block_comment >, exactly<'('> >(src);
***REMOVED***
    const char* re_pseudo_selector(const char* src) ***REMOVED***
      return sequence< identifier, optional < block_comment >, exactly<'('> >(src);
***REMOVED***
    // Match the CSS negation pseudo-class.
    const char* pseudo_not(const char* src) ***REMOVED***
      return word< pseudo_not_kwd >(src);
***REMOVED***
    // Match CSS 'odd' and 'even' keywords for functional pseudo-classes.
    const char* even(const char* src) ***REMOVED***
      return word<even_kwd>(src);
***REMOVED***
    const char* odd(const char* src) ***REMOVED***
      return word<odd_kwd>(src);
***REMOVED***
    // Match CSS attribute-matching operators.
    const char* exact_match(const char* src) ***REMOVED*** return exactly<'='>(src); ***REMOVED***
    const char* class_match(const char* src) ***REMOVED*** return exactly<tilde_equal>(src); ***REMOVED***
    const char* dash_match(const char* src) ***REMOVED*** return exactly<pipe_equal>(src); ***REMOVED***
    const char* prefix_match(const char* src) ***REMOVED*** return exactly<caret_equal>(src); ***REMOVED***
    const char* suffix_match(const char* src) ***REMOVED*** return exactly<dollar_equal>(src); ***REMOVED***
    const char* substring_match(const char* src) ***REMOVED*** return exactly<star_equal>(src); ***REMOVED***
    // Match CSS combinators.
    /* not used anymore - remove?
    const char* adjacent_to(const char* src) ***REMOVED***
      return sequence< optional_spaces, exactly<'+'> >(src);
***REMOVED***
    const char* precedes(const char* src) ***REMOVED***
      return sequence< optional_spaces, exactly<'~'> >(src);
***REMOVED***
    const char* parent_of(const char* src) ***REMOVED***
      return sequence< optional_spaces, exactly<'>'> >(src);
***REMOVED***
    const char* ancestor_of(const char* src) ***REMOVED***
      return sequence< spaces, negate< exactly<'***REMOVED***'> > >(src);
***REMOVED****/

    // Match SCSS variable names.
    const char* variable(const char* src) ***REMOVED***
      return sequence<exactly<'$'>, identifier>(src);
***REMOVED***

    // Match Sass boolean keywords.
    const char* kwd_true(const char* src) ***REMOVED***
      return word<true_kwd>(src);
***REMOVED***
    const char* kwd_false(const char* src) ***REMOVED***
      return word<false_kwd>(src);
***REMOVED***
    const char* kwd_only(const char* src) ***REMOVED***
      return keyword < only_kwd >(src);
***REMOVED***
    const char* kwd_and(const char* src) ***REMOVED***
      return keyword < and_kwd >(src);
***REMOVED***
    const char* kwd_or(const char* src) ***REMOVED***
      return keyword < or_kwd >(src);
***REMOVED***
    const char* kwd_not(const char* src) ***REMOVED***
      return keyword < not_kwd >(src);
***REMOVED***
    const char* kwd_eq(const char* src) ***REMOVED***
      return exactly<eq>(src);
***REMOVED***
    const char* kwd_neq(const char* src) ***REMOVED***
      return exactly<neq>(src);
***REMOVED***
    const char* kwd_gt(const char* src) ***REMOVED***
      return exactly<gt>(src);
***REMOVED***
    const char* kwd_gte(const char* src) ***REMOVED***
      return exactly<gte>(src);
***REMOVED***
    const char* kwd_lt(const char* src) ***REMOVED***
      return exactly<lt>(src);
***REMOVED***
    const char* kwd_lte(const char* src) ***REMOVED***
      return exactly<lte>(src);
***REMOVED***

    // match specific IE syntax
    const char* ie_progid(const char* src) ***REMOVED***
      return sequence <
        word<progid_kwd>,
        exactly<':'>,
        alternatives< identifier_schema, identifier >,
        zero_plus< sequence<
          exactly<'.'>,
          alternatives< identifier_schema, identifier >
        > >,
        zero_plus < sequence<
          exactly<'('>,
          optional_css_whitespace,
          optional < sequence<
            alternatives< variable, identifier_schema, identifier >,
            optional_css_whitespace,
            exactly<'='>,
            optional_css_whitespace,
            alternatives< variable, identifier_schema, identifier, quoted_string, number, hexa >,
            zero_plus< sequence<
              optional_css_whitespace,
              exactly<','>,
              optional_css_whitespace,
              sequence<
                alternatives< variable, identifier_schema, identifier >,
                optional_css_whitespace,
                exactly<'='>,
                optional_css_whitespace,
                alternatives< variable, identifier_schema, identifier, quoted_string, number, hexa >
              >
            > >
          > >,
          optional_css_whitespace,
          exactly<')'>
        > >
      >(src);
***REMOVED***
    const char* ie_expression(const char* src) ***REMOVED***
      return sequence < word<expression_kwd>, exactly<'('>, skip_over_scopes< exactly<'('>, exactly<')'> > >(src);
***REMOVED***
    const char* ie_property(const char* src) ***REMOVED***
      return alternatives < ie_expression, ie_progid >(src);
***REMOVED***

    // const char* ie_args(const char* src) ***REMOVED***
    //   return sequence< alternatives< ie_keyword_arg, value_schema, quoted_string, interpolant, number, identifier, delimited_by< '(', ')', true> >,
    //                    zero_plus< sequence< optional_css_whitespace, exactly<','>, optional_css_whitespace, alternatives< ie_keyword_arg, value_schema, quoted_string, interpolant, number, identifier, delimited_by<'(', ')', true> > > > >(src);
    // ***REMOVED***

    const char* ie_keyword_arg_property(const char* src) ***REMOVED***
      return alternatives <
          variable,
          identifier_schema,
          identifier
        >(src);
***REMOVED***
    const char* ie_keyword_arg_value(const char* src) ***REMOVED***
      return alternatives <
          variable,
          identifier_schema,
          identifier,
          quoted_string,
          number,
          hexa,
          sequence <
            exactly < '(' >,
            skip_over_scopes <
              exactly < '(' >,
              exactly < ')' >
            >
          >
        >(src);
***REMOVED***

    const char* ie_keyword_arg(const char* src) ***REMOVED***
      return sequence <
        ie_keyword_arg_property,
        optional_css_whitespace,
        exactly<'='>,
        optional_css_whitespace,
        ie_keyword_arg_value
      >(src);
***REMOVED***

    // Path matching functions.
    /* not used anymore - remove?
    const char* folder(const char* src) ***REMOVED***
      return sequence< zero_plus< any_char_except<'/'> >,
                       exactly<'/'> >(src);
***REMOVED***
    const char* folders(const char* src) ***REMOVED***
      return zero_plus< folder >(src);
***REMOVED****/
    /* not used anymore - remove?
    const char* chunk(const char* src) ***REMOVED***
      char inside_str = 0;
      const char* p = src;
      size_t depth = 0;
      while (true) ***REMOVED***
        if (!*p) ***REMOVED***
          return 0;
***REMOVED***
        else if (!inside_str && (*p == '"' || *p == '\'')) ***REMOVED***
          inside_str = *p;
***REMOVED***
        else if (*p == inside_str && *(p-1) != '\\') ***REMOVED***
          inside_str = 0;
***REMOVED***
        else if (*p == '(' && !inside_str) ***REMOVED***
          ++depth;
***REMOVED***
        else if (*p == ')' && !inside_str) ***REMOVED***
          if (depth == 0) return p;
          else            --depth;
***REMOVED***
        ++p;
  ***REMOVED***
      // unreachable
      return 0;
***REMOVED***
    */

    // follow the CSS spec more closely and see if this helps us scan URLs correctly
    /* not used anymore - remove?
    const char* NL(const char* src) ***REMOVED***
      return alternatives< exactly<'\n'>,
                           sequence< exactly<'\r'>, exactly<'\n'> >,
                           exactly<'\r'>,
                           exactly<'\f'> >(src);
***REMOVED****/

    const char* H(const char* src) ***REMOVED***
      return std::isxdigit(*src) ? src+1 : 0;
***REMOVED***

    const char* W(const char* src) ***REMOVED***
      return zero_plus< alternatives<
        space,
        exactly< '\t' >,
        exactly< '\r' >,
        exactly< '\n' >,
        exactly< '\f' >
      > >(src);
***REMOVED***

    const char* UUNICODE(const char* src) ***REMOVED***
      return sequence< exactly<'\\'>,
                       between<H, 1, 6>,
                       optional< W >
                       >(src);
***REMOVED***

    const char* NONASCII(const char* src) ***REMOVED***
      return nonascii(src);
***REMOVED***

    const char* ESCAPE(const char* src) ***REMOVED***
      return alternatives<
        UUNICODE,
        sequence<
          exactly<'\\'>,
          alternatives<
            NONASCII,
            escapable_character
          >
        >
      >(src);
***REMOVED***


    // const char* real_uri_prefix(const char* src) ***REMOVED***
    //   return alternatives<
    //     exactly< url_kwd >,
    //     exactly< url_prefix_kwd >
    //   >(src);
    // ***REMOVED***

    const char* real_uri_suffix(const char* src) ***REMOVED***
      return sequence< W, exactly< ')' > >(src);
***REMOVED***

    const char* real_uri_value(const char* src) ***REMOVED***
      return
      sequence<
        non_greedy<
          alternatives<
            class_char< real_uri_chars >,
            uri_character,
            NONASCII,
            ESCAPE
          >,
          alternatives<
            real_uri_suffix,
            exactly< hash_lbrace >
          >
        >
      >
      (src);
***REMOVED***

    const char* static_string(const char* src) ***REMOVED***
      const char* pos = src;
      const char * s = quoted_string(pos);
      Token t(pos, s);
      const unsigned int p = count_interval< interpolant >(t.begin, t.end);
      return (p == 0) ? t.end : 0;
***REMOVED***

    const char* unicode_seq(const char* src) ***REMOVED***
      return sequence <
        alternatives <
          exactly< 'U' >,
          exactly< 'u' >
        >,
        exactly< '+' >,
        padded_token <
          6, xdigit,
          exactly < '?' >
        >
      >(src);
***REMOVED***

    const char* static_component(const char* src) ***REMOVED***
      return alternatives< identifier,
                           static_string,
                           percentage,
                           hex,
                           exactly<'|'>,
                           // exactly<'+'>,
                           sequence < number, unit_identifier >,
                           number,
                           sequence< exactly<'!'>, word<important_kwd> >
                          >(src);
***REMOVED***

    const char* static_property(const char* src) ***REMOVED***
      return
        sequence <
          zero_plus<
            sequence <
              optional_css_comments,
              alternatives <
                exactly<','>,
                exactly<'('>,
                exactly<')'>,
                kwd_optional,
                quoted_string,
                interpolant,
                identifier,
                percentage,
                dimension,
                variable,
                alnum,
                sequence <
                  exactly <'\\'>,
                  any_char
                >
              >
            >
          >,
          lookahead <
            sequence <
              optional_css_comments,
              alternatives <
                exactly <';'>,
                exactly <'***REMOVED***'>,
                end_of_file
              >
            >
          >
        >(src);
***REMOVED***

    const char* static_value(const char* src) ***REMOVED***
      return sequence< sequence<
                         static_component,
                         zero_plus< identifier >
                       >,
                       zero_plus < sequence<
                                     alternatives<
                                       sequence< optional_spaces, alternatives<
                                         exactly < '/' >,
                                         exactly < ',' >,
                                         exactly < ' ' >
                                       >, optional_spaces >,
                                       spaces
                                     >,
                                     static_component
                       > >,
                       zero_plus < spaces >,
                       alternatives< exactly<';'>, exactly<'***REMOVED***'> >
                      >(src);
***REMOVED***

    const char* parenthese_scope(const char* src) ***REMOVED***
      return sequence <
        exactly < '(' >,
        skip_over_scopes <
          exactly < '(' >,
          exactly < ')' >
        >
      >(src);
***REMOVED***

    const char* type_selector(const char* src) ***REMOVED***
      return sequence< optional<namespace_schema>, identifier>(src);
***REMOVED***
    const char* re_type_selector(const char* src) ***REMOVED***
      return alternatives< type_selector, universal, quoted_string, dimension, percentage, number, identifier_alnums >(src);
***REMOVED***
    const char* re_type_selector2(const char* src) ***REMOVED***
      return alternatives< type_selector, universal, quoted_string, dimension, percentage, number, identifier_alnums >(src);
***REMOVED***
    const char* re_static_expression(const char* src) ***REMOVED***
      return sequence< number, optional_spaces, exactly<'/'>, optional_spaces, number >(src);
***REMOVED***

    template <size_t size, prelexer mx, prelexer pad>
    const char* padded_token(const char* src)
***REMOVED***
      size_t got = 0;
      const char* pos = src;
      while (got < size) ***REMOVED***
        if (!mx(pos)) break;
        ++ pos; ++ got;
  ***REMOVED***
      while (got < size) ***REMOVED***
        if (!pad(pos)) break;
        ++ pos; ++ got;
  ***REMOVED***
      return got ? pos : 0;
***REMOVED***

    template <size_t min, size_t max, prelexer mx>
    const char* minmax_range(const char* src)
***REMOVED***
      size_t got = 0;
      const char* pos = src;
      while (got < max) ***REMOVED***
        if (!mx(pos)) break;
        ++ pos; ++ got;
  ***REMOVED***
      if (got < min) return 0;
      if (got > max) return 0;
      return pos;
***REMOVED***

  ***REMOVED***
***REMOVED***
