#ifndef SASS_REMOVE_PLACEHOLDERS_H
#define SASS_REMOVE_PLACEHOLDERS_H

#pragma once

#include "ast.hpp"
#include "operation.hpp"

namespace Sass ***REMOVED***


    class Context;

    class Remove_Placeholders : public Operation_CRTP<void, Remove_Placeholders> ***REMOVED***

        Context&          ctx;

        void fallback_impl(AST_Node* n) ***REMOVED******REMOVED***

    public:
        Remove_Placeholders(Context&);
        virtual ~Remove_Placeholders() ***REMOVED*** ***REMOVED***

        using Operation<void>::operator();

        void operator()(Block*);
        void operator()(Ruleset*);
        void operator()(Media_Block*);
        void operator()(At_Rule*);

        template <typename U>
        void fallback(U x) ***REMOVED*** return fallback_impl(x); ***REMOVED***
***REMOVED***;

***REMOVED***

#endif
