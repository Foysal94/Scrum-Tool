#include "../ast.hpp"
#include "../context.hpp"
#include "../parser.hpp"
#include "../to_string.hpp"
#include <string>
#include <iostream>

using namespace Sass;

Context ctx = Context::Data();
To_String to_string;

Selector* selector(std::string src)
***REMOVED*** return Parser::from_c_str(src.c_str(), ctx, "", Position()).parse_selector_list(); ***REMOVED***

void spec(std::string sel)
***REMOVED*** std::cout << sel << "\t::\t" << selector(sel + ";")->specificity() << std::endl; ***REMOVED***

int main()
***REMOVED***
  spec("foo bar hux");
  spec(".foo .bar hux");
  spec("#foo .bar[hux='mux']");
  spec("a b c d e f");

  return 0;
***REMOVED***
