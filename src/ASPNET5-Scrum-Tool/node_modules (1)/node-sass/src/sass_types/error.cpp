#include <nan.h>
#include "error.h"
#include "../create_string.h"

namespace SassTypes
***REMOVED***
  Error::Error(Sass_Value* v) : SassValueWrapper(v) ***REMOVED******REMOVED***

  Sass_Value* Error::construct(const std::vector<v8::Local<v8::Value>> raw_val, Sass_Value **out) ***REMOVED***
    char const* value = "";

    if (raw_val.size() >= 1) ***REMOVED***
      if (!raw_val[0]->IsString()) ***REMOVED***
        return fail("Argument should be a string.", out);
  ***REMOVED***

      value = create_string(raw_val[0]);
***REMOVED***

    return *out = sass_make_error(value);
  ***REMOVED***

  void Error::initPrototype(v8::Local<v8::FunctionTemplate>) ***REMOVED******REMOVED***
***REMOVED***
