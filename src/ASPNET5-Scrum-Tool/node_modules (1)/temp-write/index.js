'use strict';
var fs = require('graceful-fs');
var tempfile = require('tempfile');

module.exports = function (str, ext, cb) ***REMOVED***
	if (typeof ext === 'function') ***REMOVED***
		cb = ext;
		ext = null;
	***REMOVED***

	var filePath = tempfile(ext);

	fs.writeFile(filePath, str, function (err) ***REMOVED***
		cb(err, filePath);
	***REMOVED***);
***REMOVED***;

module.exports.sync = function (str, ext) ***REMOVED***
	var filePath = tempfile(ext);
	fs.writeFileSync(filePath, str);
	return filePath;
***REMOVED***;
